'use strict';

// src/strategyPattern/index.ts
function strategyPattern(stroage, ...args) {
  for (const [condition, method] of stroage) {
    if (condition(...args)) {
      return method();
    }
  }
  return "";
}

exports.strategyPattern = strategyPattern;
