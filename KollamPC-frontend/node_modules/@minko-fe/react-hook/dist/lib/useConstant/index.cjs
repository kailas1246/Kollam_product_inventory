'use strict';

var react = require('react');

// src/useConstant/index.ts
function useConstant(init) {
  const ref = react.useRef(null);
  if (ref.current === null) {
    ref.current = init();
  }
  return ref.current;
}

exports.useConstant = useConstant;
