import { useSetState } from '../useSetState/index.js';

function useLocalStorageSetState(key, options) {
  return useSetState(options.defaultValue, {
    storage: {
      api: localStorage,
      key,
      options
      // Explicitly cast options to Options<T | undefined>
    }
  });
}

export { useLocalStorageSetState };
